import { CREATE_MEMOS, EDIT_MEMOS, DELETE_MEMOS } from './types';

export const CREATE_MEMOS = ({ title, url }) => ({
    type: CREATE_MEMOS,
    payload: {

    }
});

export const EDIT_MEMOS = ({ title, url }) => ({
    type: EDIT_MEMOS,
    payload: {

    }
});

export const DELETE_MEMOS = ({ title, url }) => ({
    type: DELETE_MEMOS,
    payload: {

    }
});
